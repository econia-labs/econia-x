---
jobs:
  echo:
    runs-on: 'ubuntu-latest'
    steps:
    - run: |
        echo "${{ toJSON(github.event) }}"
'on':
  workflow_run:
    workflows:
    - 'Housekeeping approval'
    types:
    - 'completed'
